struct research {
    pub arcane: u8,
    pub artificing: u8,
    pub biomancy: u8,
    pub demonancy: u8,
    pub harmonancy: u8,
    pub druidic: u8,
    pub elementalist: u8,
    pub necromancy: u8,
    pub realist: u8,
    pub runist: u8,
    pub voodoo: u8,
}
